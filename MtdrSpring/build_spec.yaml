version: 0.1
component: build
shell: bash
timeoutInSeconds: 3600

env:
  variables:
    APP_DIR: "MtdrSpring/backend"

steps:
  - type: Command
    name: Build backend
    command: |
      cd "$APP_DIR"
      chmod +x ./mvnw || true
      ./mvnw -ntp -DskipTests=true clean package
